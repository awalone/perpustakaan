<?php
	
			error_reporting(0);
			$kode = $_REQUEST['kode'];
			$lempar= $_REQUEST['lempar'];
			$bu = "HELLO";
			$gol = $_REQUEST['gol'];
			$bidang = $_REQUEST['bidang'];
			$kelompok = $_REQUEST['kelompok'];
			$carilangsung = $_REQUEST['carilangsung'];
			if($carilangsung<>'')
			$sql = "WHERE NAMA like '%".$carilangsung."%'";
			else
			$sql = "WHERE golongan = ".$gol." AND BIDANG = ".$bidang." AND KELOMPOK= ".$kelompok." AND SUB_KLP = ".$kode;
			
			
			$sql = mysql_query("select a.ktp_pemohon , a.id_pemohon, a.nm_pemohon, a.alm_pemohon, a.hp_pemohon, 
				b.nm_bdnusaha, b.id_bdnusaha, b.alm_bdnusaha from tbl_pemohon a LEFT JOIN tbl_bdnusaha b ON a.id_pemohon = b.id_pengurus
			");
			$sql = mysql_query("select *from tbl_pemohon where id_pemohon LIKE '%$lempar%'");
			$jum = mysql_num_rows($sql);
			echo $jum;
			if ($jum > 0) {
				
				?>
				<table class="tabelisi tabelisisimbad" style="font-size:11px" width="100%" cellspacing=0 cellpadding=0>
				<tr>
					<td>No</td>
					<td>ID Pemohon</td>
					<td>No. KTP Pemohon</td>
					<td>Nama Pemohon</td>
					<td>Nama Badan Usaha</td>
					<td></td>
				</tr>
				<?php
				$no = 1;
				while ($r = mysql_fetch_array($sql)) {
					$noKtp = $r['ktp_pemohon'];
					$id_pemohon = $r['id_pemohon'];
					$namaPemohon = $r['nm_pemohon'];
					$alamatPemohon = $r['alm_pemohon'];
					$hpPemohon = $r['hp_pemohon'];
					$id_bdnusaha = $r['id_bdnusaha'];
					?>
						<tr>
							<td><?php echo $no;?></td>
							<td><?php echo $id_pemohon;?></td>
							<td><?php echo $noKtp;?></td>
							<td><?php echo $namaPemohon;?></td>
							<td></td>
							<td>
							<input type="submit" value="Pilih" onClick="document.getElementById('isitext').value = '<?php echo $id_pemohon;?>';document.getElementById('namaPemohon').value='<?php echo $namaPemohon;?>';document.getElementById('noKtp').value = '<?php echo $noKtp;?>';document.getElementById('alamatPemohon').value = '<?php echo $alamatPemohon;?>';document.getElementById('noHp').value = '<?php echo $hpPemohon;?>';document.getElementById('idBadanUsaha').value = '<?php echo $id_bdnusaha;?>"></td>
						</tr>
					<?php
					$no++;
				}
				?>
				
</table>
				<?php
			}
			
			else {
				echo "Data Tidak Ditemukan";
			}
	
?>
